# Install this file separately from the lightweight browser/catalog environment.
# Versions verified by real Small-SFX CUDA generation on 2026-09-14 and
# compatible with the pinned official Stable Audio 3 pyproject.
# torch==2.7.1 and torchaudio==2.7.1 are installed by setup-models.ps1 from
# the explicit CUDA wheel index selected for the host.
transformers==5.17.0
huggingface-hub==1.31.0
safetensors==0.8.0
einops==0.8.2
einops-exts==0.0.4
tqdm==4.70.1
packaging==26.3
# stable-audio-3 is installed by setup-models.ps1 at the resolved source commit.
