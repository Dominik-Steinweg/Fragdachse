{
  "name": "fragdachse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audio:convert": "node scripts/convert-audio.mjs",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc && vite build",
    "check": "npm test && npm run build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^4.1.0",
    "playroomkit": "^0.0.91"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "jimp": "^1.6.1",
    "typescript": "~5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  }
}
